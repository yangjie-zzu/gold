kind: pipeline
type: docker
name: default

steps:
- name: test
  image: docker:latest
  privileged: true
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker ps

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock