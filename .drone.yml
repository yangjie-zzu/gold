kind: pipeline
type: docker
name: default

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run/docker.sock
  commands:
  - docker compose up -d

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock